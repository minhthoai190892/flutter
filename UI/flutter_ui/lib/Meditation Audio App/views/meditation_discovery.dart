import 'package:flutter/material.dart';

class MeditationDiscovery extends StatelessWidget {
  const MeditationDiscovery({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Discovery'),),
    );
  }
}