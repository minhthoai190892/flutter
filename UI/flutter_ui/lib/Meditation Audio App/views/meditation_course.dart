import 'package:flutter/material.dart';

class MeditationCourse extends StatelessWidget {
  const MeditationCourse({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Courses'),),
    );
  }
}